# Copilot Instructions

## Project Guidelines
- User prefers strictly scoped edits: only modify the explicitly selected code regions unless they ask for broader refactoring.
- Fixes must preserve existing code style, language, and formatting.